# Seed notes

Seed fixtures for the MVP live in [`src/lib/seed.ts`](/Users/darren/Desktop/Vibe cording/Liquor v.Codex/src/lib/seed.ts).

The admin console can write those documents into Firestore using the `Seed example data` action after the first admin account is created manually.

